/*Here is Maryam Riaz.
Roll No:PIAIC240367 */
//1.Declaring  a variable for storing my  favorite movie name with a string type annotation.
const favoriteMovie: string = "Housefull 4";
console.log(favoriteMovie);
//2. Create a template string to introduce yourself, including your name and age.
let myName: string = "Maryam Riaz";
let age: number = 25;
let introduction: string = `Hello, my name is ${myName} and I am ${age} years old.`;
console.log(introduction);
//3. Initialize a constant with a type annotation for your current mood as a boolean.
const currentMood: boolean = true; // true for happy, false for sad
console.log(currentMood);
// 4.Combine a user's first and last name using template literals and log the full name.
const firstName: string = "Maryam";
const lastName: string = "Riaz";
console.log(`Full Name: ${firstName} ${lastName}`);
// 5.Create a variable for a user's age with a type annotation and assign a number to it.
let userAge: number = 25;
console.log(userAge);
// 6.Use a template string to display a product name and its price.
const productName: string = "Smartphone";
const productPrice: number = 35000;
console.log(`Product: ${productName}, Price: ${productPrice}`);
//7.Declare a variable to store the number of pets you have with a number type annotation.
let numberOfPets: number = 2;
console.log(numberOfPets);
//8. Declare a variable with a boolean type annotation indicating if it is your birthday today.
const isBirthday: boolean = false;
console.log(isBirthday);
// 9.Create a template string that includes a user's first name, last name, and age with proper labels.
const userDetails: string = `First Name: ${firstName}, Last Name: ${lastName}, Age: ${userAge}`;
 console.log(userDetails);
//10.Initialize a constant to hold your birth year with a number type annotation.
const birthYear: number = 1999;
console.log(birthYear);
//11. Write a line of code that uses a template string to combine a city and country into a full address.
const city: string = "Wazirabad";
const country: string = "Pakistan";
const fullAddress: string = `${city}, ${country}`;
console.log(fullAddress);
//12. Declare a variable for today's temperature with a number type annotation and assign a value to it.
let todayTemperature: number = 30; // temperature in Celsius
console.log(todayTemperature);
